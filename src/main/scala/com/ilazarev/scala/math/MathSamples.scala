package com.ilazarev.scala.math

import math._

object MathSamples {
  def main(args: Array[String]) {
    println(sqrt(2)) // Yields 1.4142135623730951
    println(pow(2, 4)) // Yields 16.0
    println(min(3, Pi)) // Yields 3.0
    println(max(3, Pi)) // Yields 3.141592653589793
  }
}
